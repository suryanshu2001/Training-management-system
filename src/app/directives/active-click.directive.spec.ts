import { ActiveClickDirective } from './active-click.directive';

describe('ActiveClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveClickDirective();
    expect(directive).toBeTruthy();
  });
});
